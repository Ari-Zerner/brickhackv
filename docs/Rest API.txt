Auth

Subject
    GET /subject - return list of subjects as {id, name}
    GET /subject/{id} - return subject id as {id, name, description}
    POST /subject - takes {name, description} and return id
  
Opinion
    GET /subject/{id}/opinion
    POST /subject/{id}/opinion

Vote:
For opinion: GET voting options
auth + subject -> two voting options, voting id
On voting pair: POST choice
auth + voting id -> voting choice

Popularity gradient GET

All debates GET
Search debates GET
